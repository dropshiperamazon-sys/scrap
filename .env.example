# This application requires NO secrets by default -- it uses no paid
# APIs and collects no authenticated/private data. This file exists only
# in case you want to add optional configuration later.
#
# Example (optional): route Playwright's browser traffic through a proxy
# you control. Leave blank/unset otherwise.
# HTTP_PROXY=
# HTTPS_PROXY=
